#include <iostream>
#include <stack>
using namespace std;

nt main() {
    int number;
    cout << "Enter a number: ";
    cin >> number;
    
    if (number < 2) {
        cout << "No prime factors for numbers less than 2." << endl;
        return 0;
    }

    // compute and display prime factors of number

    return 0;
}
